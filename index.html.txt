<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday! | August 2, 2026</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600;700&family=Outfit:wght@300;400;500;600;700;800&family=Playfair+Display:ital,wght@0,600;0,800;1,400&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            pink: '#ff5e7e',
                            purple: '#8b5cf6',
                            rose: '#f43f5e',
                            gold: '#fbbf24',
                            dark: '#070709',
                            card: 'rgba(255, 255, 255, 0.03)'
                        }
                    },
                    fontFamily: {
                        sans: ['Outfit', 'sans-serif'],
                        serif: ['Playfair Display', 'serif'],
                        handwriting: ['Dancing Script', 'cursive']
                    }
                }
            }
        }
    </script>

    <style type="text/tailwindcss">
        @layer utilities {
            .glass-panel {
                background: rgba(18, 18, 26, 0.65);
                backdrop-filter: blur(20px);
                -webkit-backdrop-filter: blur(20px);
                border: 1px solid rgba(255, 255, 255, 0.08);
                box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
            }
            .glass-card {
                background: rgba(255, 255, 255, 0.03);
                backdrop-filter: blur(12px);
                -webkit-backdrop-filter: blur(12px);
                border: 1px solid rgba(255, 255, 255, 0.08);
            }
            .glass-card:hover {
                background: rgba(255, 255, 255, 0.07);
                border-color: rgba(255, 94, 126, 0.4);
                box-shadow: 0 10px 30px -10px rgba(255, 94, 126, 0.3);
            }
            .text-glow {
                text-shadow: 0 0 20px rgba(255, 94, 126, 0.5), 0 0 40px rgba(139, 92, 246, 0.3);
            }
            .text-glow-gold {
                text-shadow: 0 0 20px rgba(251, 191, 36, 0.6);
            }
        }
    </style>

    <style>
        /* Custom Animations & Dynamic Visuals */
        body {
            background-color: #050508;
            color: #f3f4f6;
            overflow-x: hidden;
        }

        /* Starfield background canvas */
        #bg-canvas, #confetti-canvas, #fireworks-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }
        #bg-canvas { z-index: 0; }
        #confetti-canvas { z-index: 50; }
        #fireworks-canvas { z-index: 10; pointer-events: auto; }

        /* Floating elements */
        .floating {
            animation: floating 6s ease-in-out infinite;
        }
        @keyframes floating {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-15px) rotate(1deg); }
        }

        /* Candle Flame Pulse */
        .flame {
            animation: flicker 1.2s ease-in-out infinite alternate;
            transform-origin: center bottom;
        }
        @keyframes flicker {
            0% { transform: scale(1) rotate(-2deg); opacity: 0.9; }
            50% { transform: scale(1.15) rotate(2deg); opacity: 1; filter: drop-shadow(0 0 12px #ff9e00); }
            100% { transform: scale(0.95) rotate(-1deg); opacity: 0.85; }
        }

        /* Polaroid 3D Tilt */
        .polaroid-card {
            transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
            transform-style: preserve-3d;
        }
        .polaroid-card:hover {
            transform: translateY(-10px) rotateX(4deg) rotateY(-4deg) scale(1.02);
        }

        /* Dynamic Scroll Reveal */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Glowing Pulse Border */
        .pulse-border {
            box-shadow: 0 0 0 0 rgba(255, 94, 126, 0.7);
            animation: pulse-border 2s infinite;
        }
        @keyframes pulse-border {
            0% { box-shadow: 0 0 0 0 rgba(255, 94, 126, 0.7); }
            70% { box-shadow: 0 0 0 15px rgba(255, 94, 126, 0); }
            100% { box-shadow: 0 0 0 0 rgba(255, 94, 126, 0); }
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #050508; }
        ::-webkit-scrollbar-thumb { background: #1f1f2e; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #ff5e7e; }
    </style>
</head>
<body class="font-sans antialiased selection:bg-brand-pink selection:text-white">

    <!-- Background Canvas -->
    <canvas id="bg-canvas"></canvas>
    <canvas id="confetti-canvas"></canvas>

    <!-- Audio Atmosphere Controller (Web Audio API Synthesizer) -->
    <div class="fixed bottom-6 right-6 z-50 flex items-center gap-3">
        <button id="music-toggle" onclick="toggleAudio()" class="glass-panel p-4 rounded-full text-white hover:text-brand-pink transition-all duration-300 flex items-center justify-center group shadow-2xl hover:scale-110">
            <i id="music-icon" class="fa-solid fa-music text-xl"></i>
            <span class="max-w-0 overflow-hidden whitespace-nowrap group-hover:max-w-xs transition-all duration-500 ease-in-out text-xs font-medium pl-0 group-hover:pl-2">Atmosphere Sound</span>
        </button>
    </div>

    <!-- Main Wrapper -->
    <div class="relative z-10 min-h-screen flex flex-col justify-between">

        <!-- NAV BAR -->
        <nav class="w-full py-6 px-8 flex justify-between items-center max-w-7xl mx-auto">
            <div class="flex items-center gap-2">
                <span class="w-3 h-3 rounded-full bg-brand-pink animate-ping"></span>
                <span class="font-serif italic font-bold text-xl tracking-wider text-white">August 2, 2026</span>
            </div>
            <div class="text-xs uppercase tracking-widest text-gray-400 font-semibold glass-panel px-4 py-2 rounded-full">
                Special Birthday Edition
            </div>
        </nav>

        <!-- HERO SECTION -->
        <header class="min-h-[85vh] flex flex-col justify-center items-center text-center px-4 relative max-w-5xl mx-auto">
            <div class="reveal">
                <span class="inline-block px-4 py-1.5 rounded-full bg-gradient-to-r from-brand-pink/20 to-brand-purple/20 border border-brand-pink/30 text-brand-pink text-sm font-medium mb-6 backdrop-blur-md">
                    ✨ Today is all about you
                </span>
            </div>
            
            <h1 class="reveal font-serif text-6xl md:text-8xl lg:text-9xl font-extrabold tracking-tight text-white mb-6 leading-tight">
                Happy Birthday, <br/>
                <span class="bg-gradient-to-r from-brand-pink via-purple-400 to-brand-gold bg-clip-text text-transparent text-glow">
                    My Favorite Person
                </span>
            </h1>

            <p class="reveal text-lg md:text-2xl text-gray-300 max-w-2xl font-light mb-10 leading-relaxed">
                Step into your custom interactive celebration space designed to honor your light, your smile, and another beautiful year of you.
            </p>

            <!-- Countdown / Age Milestone Ticker -->
            <div class="reveal grid grid-cols-2 md:grid-cols-4 gap-4 w-full max-w-2xl mb-12">
                <div class="glass-card p-4 rounded-2xl flex flex-col items-center">
                    <span class="text-3xl font-bold text-brand-pink" id="count-days">00</span>
                    <span class="text-xs uppercase text-gray-400 tracking-wider">Days of Joy</span>
                </div>
                <div class="glass-card p-4 rounded-2xl flex flex-col items-center">
                    <span class="text-3xl font-bold text-purple-400" id="count-hours">00</span>
                    <span class="text-xs uppercase text-gray-400 tracking-wider">Hours Bright</span>
                </div>
                <div class="glass-card p-4 rounded-2xl flex flex-col items-center">
                    <span class="text-3xl font-bold text-brand-gold" id="count-minutes">00</span>
                    <span class="text-xs uppercase text-gray-400 tracking-wider">Minutes Loved</span>
                </div>
                <div class="glass-card p-4 rounded-2xl flex flex-col items-center">
                    <span class="text-3xl font-bold text-rose-400" id="count-seconds">00</span>
                    <span class="text-xs uppercase text-gray-400 tracking-wider">Seconds Magic</span>
                </div>
            </div>

            <!-- Scroll Indicator -->
            <a href="#cake-section" class="reveal inline-flex items-center gap-2 text-sm text-gray-400 hover:text-white transition-colors duration-300">
                <span>Begin the Experience</span>
                <i class="fa-solid fa-arrow-down animate-bounce"></i>
            </a>
        </header>

        <!-- INTERACTIVE CAKE & CANDLE SECTION -->
        <section id="cake-section" class="py-24 px-4 relative z-10 max-w-4xl mx-auto text-center">
            <div class="reveal glass-panel p-8 md:p-12 rounded-3xl relative overflow-hidden">
                <div class="absolute -top-24 -left-24 w-48 h-48 bg-brand-pink/20 rounded-full blur-3xl pointer-events-none"></div>
                
                <h2 class="font-serif text-4xl md:text-5xl font-bold mb-3 text-white">Make A Wish</h2>
                <p class="text-gray-300 mb-10 text-sm md:text-base">Click or tap the candles below to blow them out and make your birthday wish!</p>

                <!-- Interactive Cake Container -->
                <div class="relative w-64 h-64 md:w-80 md:h-80 mx-auto mb-8 flex flex-col items-center justify-end">
                    
                    <!-- Candle Group -->
                    <div class="flex justify-center items-end gap-6 mb-2 z-20">
                        <!-- Candle 1 -->
                        <div class="candle-wrapper cursor-pointer flex flex-col items-center group" onclick="blowCandle(this)">
                            <div class="flame w-4 h-7 bg-gradient-to-t from-yellow-500 via-orange-400 to-white rounded-full mb-1"></div>
                            <div class="w-3 h-16 bg-gradient-to-b from-pink-300 to-pink-500 rounded-t-sm shadow-md"></div>
                        </div>
                        <!-- Candle 2 (Center Big) -->
                        <div class="candle-wrapper cursor-pointer flex flex-col items-center group -translate-y-2" onclick="blowCandle(this)">
                            <div class="flame w-5 h-8 bg-gradient-to-t from-yellow-500 via-orange-400 to-white rounded-full mb-1"></div>
                            <div class="w-3.5 h-20 bg-gradient-to-b from-purple-300 to-purple-500 rounded-t-sm shadow-md"></div>
                        </div>
                        <!-- Candle 3 -->
                        <div class="candle-wrapper cursor-pointer flex flex-col items-center group" onclick="blowCandle(this)">
                            <div class="flame w-4 h-7 bg-gradient-to-t from-yellow-500 via-orange-400 to-white rounded-full mb-1"></div>
                            <div class="w-3 h-16 bg-gradient-to-b from-blue-300 to-blue-500 rounded-t-sm shadow-md"></div>
                        </div>
                    </div>

                    <!-- Cake Graphic (CSS Formed Modern Minimalist Cake) -->
                    <div class="w-full z-10 flex flex-col items-center">
                        <!-- Top Layer -->
                        <div class="w-48 md:w-56 h-16 bg-gradient-to-r from-pink-400 via-rose-300 to-pink-400 rounded-t-3xl relative shadow-lg overflow-hidden">
                            <div class="absolute top-0 w-full h-4 bg-white/40 rounded-b-full"></div>
                            <div class="absolute bottom-2 left-4 w-3 h-3 bg-white/30 rounded-full"></div>
                            <div class="absolute bottom-3 left-12 w-2 h-2 bg-white/30 rounded-full"></div>
                            <div class="absolute bottom-2 right-6 w-3 h-3 bg-white/30 rounded-full"></div>
                        </div>
                        <!-- Bottom Layer -->
                        <div class="w-64 md:w-72 h-20 bg-gradient-to-r from-purple-500 via-indigo-400 to-purple-500 rounded-t-2xl relative shadow-2xl overflow-hidden border-t-4 border-white/20">
                            <div class="absolute top-0 w-full h-5 bg-white/30 rounded-b-full"></div>
                            <div class="flex justify-around items-center h-full pt-4">
                                <span class="w-3 h-3 rounded-full bg-yellow-300/60 inline-block"></span>
                                <span class="w-3 h-3 rounded-full bg-pink-300/60 inline-block"></span>
                                <span class="w-3 h-3 rounded-full bg-blue-300/60 inline-block"></span>
                                <span class="w-3 h-3 rounded-full bg-yellow-300/60 inline-block"></span>
                            </div>
                        </div>
                        <!-- Cake Plate -->
                        <div class="w-72 md:w-80 h-4 bg-gray-200/20 backdrop-blur-md rounded-full shadow-2xl border border-white/20"></div>
                    </div>
                </div>

                <!-- Status Banner -->
                <div id="wish-status" class="text-sm font-medium text-brand-pink tracking-wide h-6">
                    Tap all flames to extinguish & ignite fireworks!
                </div>
            </div>
        </section>

        <!-- PHOTO MEMORY GALLERY SECTION -->
        <section class="py-20 px-4 max-w-6xl mx-auto">
            <div class="text-center mb-16 reveal">
                <h2 class="font-serif text-4xl md:text-5xl font-bold text-white mb-4">Captured Moments</h2>
                <p class="text-gray-400 max-w-lg mx-auto">A visual celebration of memories, laughs, and unforgettable times together.</p>
            </div>

            <!-- Polaroid Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 px-4">
                
                <!-- Card 1 -->
                <div class="reveal polaroid-card glass-card p-5 rounded-2xl relative cursor-pointer" onclick="openMemoryModal('Unforgettable Adventures', 'Every single day spent with you turns into a story worth keeping forever. Here is to another year of discovering new horizons.', 'https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=800&q=80')">
                    <div class="w-full h-72 rounded-xl overflow-hidden mb-4 relative">
                        <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=800&q=80" alt="Memory 1" class="w-full h-full object-cover transform hover:scale-110 transition duration-700">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 hover:opacity-100 transition-opacity duration-300 flex items-end p-4">
                            <span class="text-xs text-white font-medium"><i class="fa-solid fa-expand mr-2"></i>Click to expand</span>
                        </div>
                    </div>
                    <h3 class="font-handwriting text-2xl text-white text-center">Unforgettable Adventures</h3>
                </div>

                <!-- Card 2 -->
                <div class="reveal polaroid-card glass-card p-5 rounded-2xl relative cursor-pointer md:translate-y-6" onclick="openMemoryModal('Pure Joy & Laughter', 'Your laugh has a dynamic way of making the darkest room feel endlessly bright. Never lose your sparkle.', 'https://images.unsplash.com/photo-1529156069898-49953e39b3ac?auto=format&fit=crop&w=800&q=80')">
                    <div class="w-full h-72 rounded-xl overflow-hidden mb-4 relative">
                        <img src="https://images.unsplash.com/photo-1529156069898-49953e39b3ac?auto=format&fit=crop&w=800&q=80" alt="Memory 2" class="w-full h-full object-cover transform hover:scale-110 transition duration-700">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 hover:opacity-100 transition-opacity duration-300 flex items-end p-4">
                            <span class="text-xs text-white font-medium"><i class="fa-solid fa-expand mr-2"></i>Click to expand</span>
                        </div>
                    </div>
                    <h3 class="font-handwriting text-2xl text-white text-center">Pure Joy & Laughter</h3>
                </div>

                <!-- Card 3 -->
                <div class="reveal polaroid-card glass-card p-5 rounded-2xl relative cursor-pointer" onclick="openMemoryModal('Endless Dreams', 'May this new year bring you closer to everything your heart quietly wishes for.', 'https://images.unsplash.com/photo-1519671482749-fd09be7ccebf?auto=format&fit=crop&w=800&q=80')">
                    <div class="w-full h-72 rounded-xl overflow-hidden mb-4 relative">
                        <img src="https://images.unsplash.com/photo-1519671482749-fd09be7ccebf?auto=format&fit=crop&w=800&q=80" alt="Memory 3" class="w-full h-full object-cover transform hover:scale-110 transition duration-700">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 hover:opacity-100 transition-opacity duration-300 flex items-end p-4">
                            <span class="text-xs text-white font-medium"><i class="fa-solid fa-expand mr-2"></i>Click to expand</span>
                        </div>
                    </div>
                    <h3 class="font-handwriting text-2xl text-white text-center">Endless Dreams</h3>
                </div>

            </div>
        </section>

        <!-- TYPEWRITER HEARTFELT LETTER SECTION -->
        <section class="py-24 px-4 max-w-4xl mx-auto">
            <div class="reveal glass-panel p-8 md:p-14 rounded-3xl relative">
                <div class="flex items-center gap-3 mb-6">
                    <i class="fa-solid fa-envelope-open-text text-brand-pink text-2xl"></i>
                    <h3 class="font-serif text-2xl md:text-3xl font-bold text-white">A Note For You</h3>
                </div>

                <!-- Envelope / Typewriter Container -->
                <div class="min-h-[220px] text-gray-300 font-sans text-base md:text-lg leading-relaxed border-l-2 border-brand-pink/50 pl-6 my-6 italic">
                    <p id="typewriter-text"></p>
                    <span id="cursor" class="inline-block w-2 h-5 bg-brand-pink ml-1 animate-pulse"></span>
                </div>

                <div class="flex justify-end pt-4">
                    <p class="font-handwriting text-3xl text-brand-pink">With all my heart ❤️</p>
                </div>
            </div>
        </section>

        <!-- SECRET REVEAL CAPSULE -->
        <section class="py-16 px-4 max-w-4xl mx-auto text-center">
            <div class="reveal">
                <h3 class="font-serif text-3xl font-bold text-white mb-6">Interactive Wish Cards</h3>
                <p class="text-gray-400 text-sm mb-10">Tap an envelope to reveal a custom note written for your special day.</p>
                
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
                    <!-- Note 1 -->
                    <div class="glass-card p-6 rounded-2xl cursor-pointer hover:border-brand-pink transition-all duration-300" onclick="revealSecret(this, '💫 May your year be filled with spontaneous adventures and quiet peace.')">
                        <i class="fa-solid fa-star text-3xl text-brand-gold mb-4"></i>
                        <h4 class="font-semibold text-white mb-2">Open Card 1</h4>
                        <p class="secret-content text-xs text-gray-400 italic">Tap to flip</p>
                    </div>
                    <!-- Note 2 -->
                    <div class="glass-card p-6 rounded-2xl cursor-pointer hover:border-brand-pink transition-all duration-300" onclick="revealSecret(this, '🌸 Thank you for bringing warmth and light into every room you enter.')">
                        <i class="fa-solid fa-heart text-3xl text-brand-pink mb-4"></i>
                        <h4 class="font-semibold text-white mb-2">Open Card 2</h4>
                        <p class="secret-content text-xs text-gray-400 italic">Tap to flip</p>
                    </div>
                    <!-- Note 3 -->
                    <div class="glass-card p-6 rounded-2xl cursor-pointer hover:border-brand-pink transition-all duration-300" onclick="revealSecret(this, '🚀 Here is to chasing your biggest goals — I will always be cheering for you!')">
                        <i class="fa-solid fa-rocket text-3xl text-purple-400 mb-4"></i>
                        <h4 class="font-semibold text-white mb-2">Open Card 3</h4>
                        <p class="secret-content text-xs text-gray-400 italic">Tap to flip</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- INTERACTIVE FIREWORKS SKY STAGE -->
        <section class="relative min-h-[60vh] flex flex-col justify-center items-center text-center px-4 my-12">
            <canvas id="fireworks-canvas"></canvas>
            
            <div class="relative z-20 pointer-events-none max-w-2xl">
                <h2 class="font-serif text-4xl md:text-6xl font-bold text-white mb-4 text-glow-gold">Light Up The Sky</h2>
                <p class="text-gray-300 text-base md:text-lg mb-8">Click or tap anywhere on the screen above to launch dynamic custom fireworks in celebration of August 2, 2026!</p>
                <button onclick="triggerMassiveFireworks()" class="pointer-events-auto px-8 py-4 rounded-full bg-gradient-to-r from-brand-pink to-brand-purple text-white font-bold tracking-wide shadow-lg hover:shadow-brand-pink/50 hover:scale-105 transition duration-300 pulse-border">
                    <i class="fa-solid fa-wand-magic-sparkles mr-2"></i> Launch Birthday Finale
                </button>
            </div>
        </section>

        <!-- FOOTER -->
        <footer class="py-8 text-center text-gray-500 text-xs border-t border-white/5 relative z-10">
            <p>Crafted with love for an unforgettable birthday celebration | August 2, 2026</p>
        </footer>
    </div>

    <!-- MEMORY MODAL -->
    <div id="memory-modal" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-md opacity-0 pointer-events-none transition-opacity duration-300">
        <div class="glass-panel p-6 md:p-8 rounded-3xl max-w-xl w-full relative transform scale-95 transition-transform duration-300" id="modal-box">
            <button onclick="closeMemoryModal()" class="absolute top-4 right-4 text-gray-400 hover:text-white text-xl p-2">
                <i class="fa-solid fa-xmark"></i>
            </button>
            <img id="modal-img" src="" alt="Expanded Memory" class="w-full h-64 object-cover rounded-2xl mb-6 shadow-xl">
            <h3 id="modal-title" class="font-serif text-2xl font-bold text-white mb-2"></h3>
            <p id="modal-desc" class="text-gray-300 text-sm leading-relaxed mb-6"></p>
            <div class="flex justify-end">
                <button onclick="closeMemoryModal()" class="px-5 py-2 rounded-xl bg-white/10 hover:bg-white/20 text-white text-xs font-semibold">Close</button>
            </div>
        </div>
    </div>

    <!-- JAVASCRIPT LOGIC -->
    <script>
        /* ------------------------------------------------------------------
           1. STARFIELD BACKGROUND CANVAS
        ------------------------------------------------------------------ */
        const bgCanvas = document.getElementById('bg-canvas');
        const bgCtx = bgCanvas.getContext('2d');
        let stars = [];

        function resizeBgCanvas() {
            bgCanvas.width = window.innerWidth;
            bgCanvas.height = window.innerHeight;
        }
        window.addEventListener('resize', resizeBgCanvas);
        resizeBgCanvas();

        class Star {
            constructor() {
                this.reset();
            }
            reset() {
                this.x = Math.random() * bgCanvas.width;
                this.y = Math.random() * bgCanvas.height;
                this.size = Math.random() * 1.8 + 0.2;
                this.alpha = Math.random() * 0.8 + 0.2;
                this.speed = Math.random() * 0.05 + 0.01;
            }
            update() {
                this.alpha += Math.sin(Date.now() * this.speed) * 0.01;
                if (this.alpha <= 0) this.reset();
            }
            draw() {
                bgCtx.fillStyle = `rgba(255, 255, 255, ${Math.abs(this.alpha)})`;
                bgCtx.beginPath();
                bgCtx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                bgCtx.fill();
            }
        }

        for (let i = 0; i < 150; i++) stars.push(new Star());

        function animateBg() {
            bgCtx.clearRect(0, 0, bgCanvas.width, bgCanvas.height);
            stars.forEach(star => {
                star.update();
                star.draw();
            });
            requestAnimationFrame(animateBg);
        }
        animateBg();

        /* ------------------------------------------------------------------
           2. COUNTDOWN / TIMER COUNTER LOGIC
        ------------------------------------------------------------------ */
        // Calculate relative metrics or dynamic celebration target
        function updateCounter() {
            const now = new Date();
            // Display live dynamic metrics
            document.getElementById('count-days').innerText = Math.floor((now.getTime() / (1000 * 60 * 60 * 24)) % 365);
            document.getElementById('count-hours').innerText = String(now.getHours()).padStart(2, '0');
            document.getElementById('count-minutes').innerText = String(now.getMinutes()).padStart(2, '0');
            document.getElementById('count-seconds').innerText = String(now.getSeconds()).padStart(2, '0');
        }
        setInterval(updateCounter, 1000);
        updateCounter();

        /* ------------------------------------------------------------------
           3. CANDLE BLOWING & CONFETTI ENGINE
        ------------------------------------------------------------------ */
        let blownCandlesCount = 0;
        const totalCandles = 3;

        function blowCandle(element) {
            const flame = element.querySelector('.flame');
            if (flame && flame.style.display !== 'none') {
                flame.style.display = 'none';
                blownCandlesCount++;
                
                // Play subtle chime sound via Audio Synth
                playChimeSound();

                if (blownCandlesCount === totalCandles) {
                    document.getElementById('wish-status').innerHTML = "✨ Your wish is sent to the stars! Happy Birthday! ✨";
                    document.getElementById('wish-status').classList.add('text-glow-gold', 'text-lg');
                    triggerConfetti();
                    triggerMassiveFireworks();
                }
            }
        }

        // Confetti Canvas Particle System
        const confettiCanvas = document.getElementById('confetti-canvas');
        const confettiCtx = confettiCanvas.getContext('2d');
        let confettiParticles = [];

        function resizeConfettiCanvas() {
            confettiCanvas.width = window.innerWidth;
            confettiCanvas.height = window.innerHeight;
        }
        window.addEventListener('resize', resizeConfettiCanvas);
        resizeConfettiCanvas();

        class Confetti {
            constructor() {
                this.x = Math.random() * confettiCanvas.width;
                this.y = -20;
                this.size = Math.random() * 8 + 4;
                this.color = ['#ff5e7e', '#8b5cf6', '#fbbf24', '#06b6d4', '#f43f5e'][Math.floor(Math.random() * 5)];
                this.speedY = Math.random() * 3 + 2;
                this.speedX = Math.random() * 2 - 1;
                this.rotation = Math.random() * 360;
                this.rotationSpeed = Math.random() * 10 - 5;
            }
            update() {
                this.y += this.speedY;
                this.x += this.speedX;
                this.rotation += this.rotationSpeed;
            }
            draw() {
                confettiCtx.save();
                confettiCtx.translate(this.x, this.y);
                confettiCtx.rotate((this.rotation * Math.PI) / 180);
                confettiCtx.fillStyle = this.color;
                confettiCtx.fillRect(-this.size / 2, -this.size / 2, this.size, this.size);
                confettiCtx.restore();
            }
        }

        function triggerConfetti() {
            for (let i = 0; i < 120; i++) {
                confettiParticles.push(new Confetti());
            }
        }

        function animateConfetti() {
            confettiCtx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);
            confettiParticles.forEach((p, index) => {
                p.update();
                p.draw();
                if (p.y > confettiCanvas.height) {
                    confettiParticles.splice(index, 1);
                }
            });
            requestAnimationFrame(animateConfetti);
        }
        animateConfetti();

        /* ------------------------------------------------------------------
           4. FIREWORKS SKY STAGE CANVAS
        ------------------------------------------------------------------ */
        const fwCanvas = document.getElementById('fireworks-canvas');
        const fwCtx = fwCanvas.getContext('2d');
        let fireworks = [];
        let fwParticles = [];

        function resizeFwCanvas() {
            fwCanvas.width = window.innerWidth;
            fwCanvas.height = window.innerHeight;
        }
        window.addEventListener('resize', resizeFwCanvas);
        resizeFwCanvas();

        class Firework {
            constructor(sx, sy, tx, ty) {
                this.x = sx;
                this.y = sy;
                this.sx = sx;
                this.sy = sy;
                this.tx = tx;
                this.ty = ty;
                this.distanceToTarget = Math.hypot(sx - tx, sy - ty);
                this.distanceTraveled = 0;
                this.coordinates = [];
                this.coordinateCount = 3;
                while (this.coordinateCount--) {
                    this.coordinates.push([this.x, this.y]);
                }
                this.angle = Math.atan2(ty - sy, tx - sx);
                this.speed = 2;
                this.acceleration = 1.05;
                this.hue = Math.random() * 360;
            }
            update(index) {
                this.coordinates.pop();
                this.coordinates.unshift([this.x, this.y]);
                this.speed *= this.acceleration;
                const vx = Math.cos(this.angle) * this.speed;
                const vy = Math.sin(this.angle) * this.speed;
                this.distanceTraveled = Math.hypot(this.sx - (this.x + vx), this.sy - (this.y + vy));

                if (this.distanceTraveled >= this.distanceToTarget) {
                    createFwParticles(this.tx, this.ty, this.hue);
                    fireworks.splice(index, 1);
                } else {
                    this.x += vx;
                    this.y += vy;
                }
            }
            draw() {
                fwCtx.beginPath();
                fwCtx.moveTo(this.coordinates[this.coordinates.length - 1][0], this.coordinates[this.coordinates.length - 1][1]);
                fwCtx.lineTo(this.x, this.y);
                fwCtx.strokeStyle = `hsl(${this.hue}, 100%, 60%)`;
                fwCtx.stroke();
            }
        }

        class FwParticle {
            constructor(x, y, hue) {
                this.x = x;
                this.y = y;
                this.coordinates = [];
                this.coordinateCount = 5;
                while (this.coordinateCount--) {
                    this.coordinates.push([this.x, this.y]);
                }
                this.angle = Math.random() * Math.PI * 2;
                this.speed = Math.random() * 10 + 1;
                this.friction = 0.95;
                this.gravity = 1;
                this.hue = Math.random() * 20 + hue;
                this.brightness = Math.random() * 50 + 50;
                this.alpha = 1;
                this.decay = Math.random() * 0.03 + 0.015;
            }
            update(index) {
                this.coordinates.pop();
                this.coordinates.unshift([this.x, this.y]);
                this.speed *= this.friction;
                this.x += Math.cos(this.angle) * this.speed;
                this.y += Math.sin(this.angle) * this.speed + this.gravity;
                this.alpha -= this.decay;
                if (this.alpha <= this.decay) {
                    fwParticles.splice(index, 1);
                }
            }
            draw() {
                fwCtx.beginPath();
                fwCtx.moveTo(this.coordinates[this.coordinates.length - 1][0], this.coordinates[this.coordinates.length - 1][1]);
                fwCtx.lineTo(this.x, this.y);
                fwCtx.strokeStyle = `hsla(${this.hue}, 100%, ${this.brightness}%, ${this.alpha})`;
                fwCtx.stroke();
            }
        }

        function createFwParticles(x, y, hue) {
            let particleCount = 40;
            while (particleCount--) {
                fwParticles.push(new FwParticle(x, y, hue));
            }
        }

        function animateFireworks() {
            fwCtx.globalCompositeOperation = 'destination-out';
            fwCtx.fillStyle = 'rgba(0, 0, 0, 0.2)';
            fwCtx.fillRect(0, 0, fwCanvas.width, fwCanvas.height);
            fwCtx.globalCompositeOperation = 'lighter';

            let i = fireworks.length;
            while (i--) {
                fireworks[i].draw();
                fireworks[i].update(i);
            }
            let j = fwParticles.length;
            while (j--) {
                fwParticles[j].draw();
                fwParticles[j].update(j);
            }
            requestAnimationFrame(animateFireworks);
        }
        animateFireworks();

        fwCanvas.addEventListener('click', (e) => {
            const rect = fwCanvas.getBoundingClientRect();
            const tx = e.clientX - rect.left;
            const ty = e.clientY - rect.top;
            fireworks.push(new Firework(fwCanvas.width / 2, fwCanvas.height, tx, ty));
        });

        function triggerMassiveFireworks() {
            for (let i = 0; i < 5; i++) {
                setTimeout(() => {
                    const tx = Math.random() * (fwCanvas.width * 0.8) + fwCanvas.width * 0.1;
                    const ty = Math.random() * (fwCanvas.height * 0.5) + 100;
                    fireworks.push(new Firework(fwCanvas.width / 2, fwCanvas.height, tx, ty));
                }, i * 300);
            }
        }

        /* ------------------------------------------------------------------
           5. TYPEWRITER LETTER REVEAL (SCROLL TRIGGERED)
        ------------------------------------------------------------------ */
        const letterText = "On this extraordinary day—August 2, 2026—I want to remind you just how deeply appreciated and celebrated you are. You bring a bright, effortless magic to every moment and everyone around you. May this year shower you with unforgettable happiness, growth, and endless reasons to smile. Happy Birthday!";
        let typewriterIndex = 0;
        let typewriterStarted = false;

        function startTypewriter() {
            if (typewriterStarted) return;
            typewriterStarted = true;
            const element = document.getElementById('typewriter-text');
            
            function type() {
                if (typewriterIndex < letterText.length) {
                    element.innerHTML += letterText.charAt(typewriterIndex);
                    typewriterIndex++;
                    setTimeout(type, 35);
                } else {
                    document.getElementById('cursor').style.display = 'none';
                }
            }
            type();
        }

        /* ------------------------------------------------------------------
           6. INTERACTIVE REVEAL & MODAL HANDLERS
        ------------------------------------------------------------------ */
        function openMemoryModal(title, desc, imgSrc) {
            document.getElementById('modal-title').innerText = title;
            document.getElementById('modal-desc').innerText = desc;
            document.getElementById('modal-img').src = imgSrc;
            const modal = document.getElementById('memory-modal');
            const box = document.getElementById('modal-box');
            modal.classList.remove('opacity-0', 'pointer-events-none');
            box.classList.remove('scale-95');
            box.classList.add('scale-100');
        }

        function closeMemoryModal() {
            const modal = document.getElementById('memory-modal');
            const box = document.getElementById('modal-box');
            modal.classList.add('opacity-0', 'pointer-events-none');
            box.classList.remove('scale-100');
            box.classList.add('scale-95');
        }

        function revealSecret(element, text) {
            const secretContent = element.querySelector('.secret-content');
            secretContent.innerText = text;
            secretContent.classList.remove('italic', 'text-gray-400');
            secretContent.classList.add('text-brand-pink', 'font-medium', 'text-sm', 'mt-2');
            element.classList.add('border-brand-pink', 'scale-105');
            playChimeSound();
        }

        /* ------------------------------------------------------------------
           7. WEB AUDIO API SYNTHESIZER (ATMOSPHERE & SOUND EFFECTS)
        ------------------------------------------------------------------ */
        let audioCtx = null;
        let isPlayingAudio = false;
        let melodyInterval = null;

        function initAudio() {
            if (!audioCtx) {
                audioCtx = new (window.AudioContext || window.webkitAudioContext)();
            }
        }

        function playTone(freq, duration, type = 'sine') {
            if (!audioCtx) return;
            const osc = audioCtx.createOscillator();
            const gain = audioCtx.createGain();
            osc.type = type;
            osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
            gain.gain.setValueAtTime(0.08, audioCtx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.0001, audioCtx.currentTime + duration);
            osc.connect(gain);
            gain.connect(audioCtx.destination);
            osc.start();
            osc.stop(audioCtx.currentTime + duration);
        }

        function playChimeSound() {
            initAudio();
            if (audioCtx) {
                playTone(523.25, 0.4); // C5
                setTimeout(() => playTone(659.25, 0.4), 120); // E5
                setTimeout(() => playTone(783.99, 0.6), 240); // G5
            }
        }

        function toggleAudio() {
            initAudio();
            const icon = document.getElementById('music-icon');
            
            if (!isPlayingAudio) {
                audioCtx.resume();
                isPlayingAudio = true;
                icon.classList.remove('fa-music');
                icon.classList.add('fa-pause', 'text-brand-pink');
                
                // Dreamy Ambient Harmonic Arpeggio Engine
                const notes = [261.63, 329.63, 392.00, 493.88, 523.25, 659.25]; // C major 7th tones
                let step = 0;
                melodyInterval = setInterval(() => {
                    const freq = notes[step % notes.length];
                    playTone(freq, 1.8, 'sine');
                    step++;
                }, 800);
            } else {
                isPlayingAudio = false;
                clearInterval(melodyInterval);
                icon.classList.remove('fa-pause', 'text-brand-pink');
                icon.classList.add('fa-music');
            }
        }

        /* ------------------------------------------------------------------
           8. SCROLL OBSERVER REVEAL ANIMATION
        ------------------------------------------------------------------ */
        const observerOptions = {
            threshold: 0.15
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                    if (entry.target.querySelector('#typewriter-text')) {
                        startTypewriter();
                    }
                }
            });
        }, observerOptions);

        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    </script>
</body>
</html>